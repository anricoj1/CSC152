# Larger Than n
# write a function that accepts two arguments:
# a list
# a number n
n = [4,5,6,7,1,3,7,5]

x = [i for i in n if i > 5]

len(x)

def length_list(list_numbers, number):
    x = [i for i in list_numbers if n > number]
    return len(x)
length_list(n, 5)

length_list()
